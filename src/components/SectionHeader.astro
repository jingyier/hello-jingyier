---
interface Props {
  eyebrow: string;
  title: string;
  summary?: string;
}

const { eyebrow, title, summary } = Astro.props;
---

<header class="section-header">
  <p class="eyebrow">{eyebrow}</p>
  <h2>{title}</h2>
  {summary && <p>{summary}</p>}
</header>
